#include <stdio.h>
int main ()
{ 
   int n,i,b ;
   int cont=0;

   printf("Ingrese el numero de elementos del arreglo: ");
    scanf("%d", &n);
   int arreglo[n];

   for ( i=0; i<n; i++)
   {
      printf("Ingrese el  %d numero:" , i+1);
      scanf("%d", &arreglo[i]);
   }
    for ( i=0; i<n; i++)
     {
        if ( i==0)
