#include <stdio.h>
int main ()
{ 
   int n,i,b ;
   int nulo=0, positivos=0, negativos=0;

   printf("Ingrese el numero de elementos del arreglo: ");
    scanf("%d", &n);
   int arreglo[n];

   for ( i=0; i<n; i++)
   {
      printf("Ingrese el  %d numero:" , i+1);
      scanf("%d", &arreglo[i]);
   }
    for ( i=0; i<n; i++)
     {
        if ( arreglo[i]==0)
         { 
            nulo= nulo+1;
         }

          else if ( arreglo[i]>0)
      { 
        positivos= positivos+1;
      }
        else 
        {  
            negativos=negativos+1;          
         } 
     
   }
   
printf(" Numero de nulos %d\n", nulo);
printf("Numero de positivos %d\n", positivos);
printf("Numero de negativos %d\n", negativos);
return 0;
}

