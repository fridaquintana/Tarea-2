#include <stdio.h>
int main ()
{ 
   int i;
   int n=0;
  int pares[100];

   for ( i=0; i<100; i++)
    { 
      pares[i] = n;
      n=n+2;
    }
     printf("Los 100 primeros numeros pares son:\n");
      for (i = 0; i < 100; i++) 
        {
        printf("%d ", pares[i]);
        }
return 0;
}
