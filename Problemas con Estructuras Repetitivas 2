#include <stdio.h>
int main ()
{  
  int num=0, i=1, nulo=0, positivos=0, negativos=0; 
  printf( "ingrese el numero desde el cual se quiere conocer sus 100 numeros positivos y negativos:");
  scanf("%d", &num);

  for( i=1; i<=100; i++)
  {
    if( num==0)
    {
        nulo= nulo+1;
    }
      else if ( num>0)
      { 
        positivos= positivos+1;
      }
        else 
        {  
            negativos=negativos+1;          
         } 
      num=num+1;
    }

printf(" Numero de nulos %d\n", nulo);
printf("Numero de positivos %d\n", positivos);
printf("Numero de negativos %d\n", negativos);
return 0;
}
