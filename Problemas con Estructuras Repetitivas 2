#include <stdio.h>
int main ()
{  
  int num=0, i=1, nulo=0, positivos=0, negativos=0; 
  printf( "ingrese el numero desde el cual se quiere conocer sus 100 numeros naturales positivos y negativos:");
  scanf("%d", &num);

  for( i=1; i<=100; i++)
  {
    if( num=0)
    {
        nulo= num+1;
        printf(" Numero de nulos %d\n", nulo);
    }
      else if ( num>0)
      { 
        positivos= num+1;
        printf("Numero de positivos %d\n", positivos);
      }
        else
        {  
            negativos: num+1;
            printf("Numero de negativos %d\n", negativos);
         } 
    }
return 0;
}
